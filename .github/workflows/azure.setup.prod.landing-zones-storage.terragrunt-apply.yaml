on:
  push:
    branches:
      - main
    paths:
      - 'azure/setup/prod/westeurope/multitenant/landing-zones-storage/**'
  workflow_dispatch:
    inputs:
      logLevel:
        description: 'Log level'
        required: true
        default: 'INFO'
        type: choice
        options:
        - INFO
        - WARNING
        - DEBUG

concurrency: ci-${{ github.workflow }}-${{ github.ref }}

jobs:
  call-terraform-apply-workflow:
    uses: JamieTaffurelliOrg/github-actions-templates/.github/workflows/azure-terragrunt-run-all-apply-template.yaml@main
    with:
      terragrunt-working-dir: './azure/setup/prod/westeurope/multitenant/landing-zones-storage'
      environment: azure.setup.prod.landing-zones.deploy
      log-level: ${{ inputs.logLevel }}
    secrets: inherit
