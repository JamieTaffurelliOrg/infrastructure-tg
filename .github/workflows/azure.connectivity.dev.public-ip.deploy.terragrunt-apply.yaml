on:
  push:
    branches:
      - main
    paths:
      - 'azure/connectivity/dev/westeurope/multitenant/public-ip**'
  workflow_dispatch:
    inputs:
      logLevel:
        description: 'Log level'
        required: true
        default: 'INFO'
        type: choice
        options:
        - INFO
        - WARNING
        - DEBUG

concurrency: ci-${{ github.workflow }}-${{ github.ref }}

jobs:
  call-terraform-apply-workflow:
    uses: JamieTaffurelliOrg/github-actions-templates/.github/workflows/azure-terragrunt-apply-template.yaml@main
    with:
      terragrunt-working-dir: './azure/connectivity/dev/westeurope/multitenant/public-ip'
      environment: connectivity.dev.public-ip.deploy
      log-level: ${{ inputs.logLevel }}
    secrets: inherit
